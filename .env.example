# ============================================================
# .env.example — WH-ED Mentorship App
# ============================================================
# Copy this file to .env.local and fill in your values.
#
# RENDER DEPLOYMENT:
#   - DATABASE_URL: auto-set via render.yaml fromDatabase
#   - JWT_SECRET: generate with: openssl rand -hex 32
#   - NODE_ENV: production
# ============================================================

# ── Database ────────────────────────────────────────────────
# Local: your local PostgreSQL
# Render: auto-set via render.yaml (leave as placeholder)
DATABASE_URL=postgresql://neondb_owner:npg_Bgwf9bDVRM4r@ep-lingering-feather-aozws6vi.c-2.ap-southeast-1.aws.neon.tech/neondb?sslmode=require

# ── Authentication ───────────────────────────────────────────
# Generate with: openssl rand -hex 32
JWT_SECRET=change_me_generate_a_real_secret

# ── App URL (for production) ─────────────────────────────────
NEXT_PUBLIC_APP_URL=https://wh-ed-mentorship.onrender.com